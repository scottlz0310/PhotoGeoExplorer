<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoGeoExplorer.Panes.Map"
    xmlns:mapsui="using:Mapsui.UI.WinUI">

    <DataTemplate x:Key="MapPaneViewTemplate" x:DataType="local:MapPaneViewModel">
        <Grid>
            <!-- 地図コントロール -->
            <mapsui:MapControl x:Name="MapControl" Map="{x:Bind Map, Mode=OneWay}" />

            <!-- ステータスオーバーレイ -->
            <Grid
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{x:Bind StatusVisibility, Mode=OneWay}">
                <Border
                    Padding="24"
                    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <StackPanel Spacing="12">
                        <SymbolIcon Symbol="{x:Bind StatusIcon, Mode=OneWay}" />
                        <TextBlock
                            FontSize="16"
                            FontWeight="SemiBold"
                            Text="{x:Bind StatusTitle, Mode=OneWay}"
                            TextAlignment="Center" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{x:Bind StatusDetail, Mode=OneWay}"
                            TextAlignment="Center"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
