<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="SettingsPaneTemplate">
        <Grid Padding="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- ヘッダー -->
            <TextBlock
                Grid.Row="0"
                Text="{Binding Title}"
                FontSize="20"
                FontWeight="SemiBold"
                Margin="0,0,0,16" />

            <!-- 言語設定（サンプル） -->
            <StackPanel Grid.Row="1" Spacing="8" Margin="0,0,0,16">
                <TextBlock Text="Language" FontWeight="SemiBold" />
                <TextBlock Text="{Binding LanguageSetting}" Opacity="0.7" />
            </StackPanel>

            <!-- テーマ設定（サンプル） -->
            <StackPanel Grid.Row="2" Spacing="8" Margin="0,0,0,16">
                <TextBlock Text="Theme" FontWeight="SemiBold" />
                <TextBlock Text="{Binding ThemeSetting}" Opacity="0.7" />
            </StackPanel>

            <!-- 説明 -->
            <Border
                Grid.Row="3"
                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                CornerRadius="8"
                Padding="16"
                VerticalAlignment="Top">
                <StackPanel Spacing="8">
                    <TextBlock
                        Text="このPaneはサンプル実装です"
                        FontWeight="SemiBold" />
                    <TextBlock
                        Text="実際の設定機能は MainWindow に実装されています。このPaneは将来的な移行のためのテンプレートとして使用できます。"
                        TextWrapping="Wrap"
                        Opacity="0.7" />
                </StackPanel>
            </Border>
        </Grid>
    </DataTemplate>
</ResourceDictionary>
